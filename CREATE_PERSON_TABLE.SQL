CREATE TABLE test.person (
                             id     INT (11) NOT NULL,
                             name   VARCHAR (128) NOT NULL,
                             PRIMARY KEY (id)
)
    ENGINE = INNODB
CHARACTER SET utf8
COLLATE utf8_general_ci;